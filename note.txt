The build errors reference files that don't actually exist in this project (src/hooks/useComments.ts, src/lib/seed.ts, src/pages/Articles.tsx, src/pages/Categories.tsx, src/pages/CategoryPage.tsx, src/pages/HomePage.tsx, src/pages/WritePage.tsx). The actual project files (Home.tsx, Category.tsx, PostPage.tsx, etc.) are correct and reference the right exports.

All the existing project files already use the correct imports (POSTS, getCategories, getPostBySlug, etc.) so the build should succeed once the stale files are not part of the tree.
